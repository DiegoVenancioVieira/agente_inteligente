# Segredos e credenciais - NUNCA versionar
.env
.env.local
*credenciais*
*credentials*
*.key
*.pem

# Banco de runtime: cache de perguntas de cidadaos (LGPD) + indice de intencoes.
# E' regeneravel (POST /intent/reindex) e nao deve sair da VPS.
cache.db
*.db

# Dependencias
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Sistema
.DS_Store
Thumbs.db
